spring:
  application:
    name: dcs
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/dcs
    username: whtjdghks03
    ## username, password 전부 본인 껄로 바꾸기
    password: navy3fleet
  mail:
    host: smtp.gmail.com
    port: 587
    username: gyust941326@gmail.com
    password: pantppqokveizzpo
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  thymeleaf:
    cache: false
mybatis:
  mapper-locations: classpath:mappers/*.xml
  config-location: classpath:mappers/config/configuration.xml
